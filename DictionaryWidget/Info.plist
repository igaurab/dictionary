<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- INFOPLIST_KEY_NSExtensionPointIdentifier is silently dropped by this
	     Xcode's Info.plist generator, so the extension point is declared in a
	     real file. GENERATE_INFOPLIST_FILE stays on and merges the rest. -->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
